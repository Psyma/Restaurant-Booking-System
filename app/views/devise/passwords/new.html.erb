<%= render "shared/navbar"%> 
<div class="hero-header d-flex justify-content-center align-items-center"  id="content">  
    <div class="container">
        <div class="row d-flex justify-content-center align-items-center">
            <div class="col col-xl-10">
                <div class="card bg-dark" style="border-radius: 1rem;">
                    <div class="row g-0">
                        <div class="col-md-6 col-lg-6 d-flex align-items-center"> 
                            <%= image_tag "img/hero.png", class: "img-fluid d-none d-md-block" %> 
                        </div>
                        <div class="col-md-6 col-lg-6 d-flex align-items-center">
                            <div class="card-body p-4 p-lg-5 text-black"> 
                                <h3 class="text-light mb-4">Forgot your password?</h3>
                                <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |form| %>  
                                    <div class="form-floating mb-4">
                                        <%= email_field_tag :email, nil, name: field_name(nil, "user", :email), class: "form-control", placeholder: "Email address", autofocus: true %>
                                        <div class="pull-right text-danger">
                                            <%= inline_error_for(:email, resource) %>
                                        </div>
                                        <%= label_tag :email, "Email address" %>
                                    </div>

                                    <%= form.submit "Send me reset password instruction", class: "btn btn-success btn-block mt-2", style: "width: 100%" %>  
                                    <p class="mt-2 text-light">
                                        Already have an account? <%= link_to "login here", login_path, class: "text-info" %> 
                                    </p>
                                <% end %> 
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>