<div class="bg-white p-0 min-vh-100 d-flex flex-column justify-content-between">
    <%= render "shared/navbar" %>   
        <div class="container ">
            <% if Current.user.role == Roles::ADMIN %> 
                <%= link_to "Products", products_path, class: " " %>
            <% end %> 
            <div class="text-center">
                <h5 class="section-title ff-secondary text-center text-primary fw-normal">Profile Section</h5>
                <h1 class="mb-3"><%= Current.user.full_name %></h1>
            </div>  
        </div> 
    <%= render "shared/footer" %> 
</div>